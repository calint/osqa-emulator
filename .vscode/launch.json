{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/osqa",
            "args": [
                "firmware.img",
                "sdcard.img",
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
        }
    ]
}